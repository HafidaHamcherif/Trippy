import React from 'react';

class Home extends React.Component{
    
    render() {
        const {
            cities
        } =this.props
        console.log(cities)
        // if(this.props.cities.length > 0){
            
        // }
        return(
            <div>
               
            </div>

        );
    }
}

export default Home;